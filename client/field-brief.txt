PROCHARACTERS CLOUD — FIELD BRIEF
v1.0.0 · Phase 20 · Innovation Lanes 1–4
Boss Sr. · King Grok · 30 workers

SAVE THIS FILE on your phone (download, email to self, Files app).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INNOVATION — ACTIVE NOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

LANE 1 · REAL (wire + live activate — no restart)
  If you already have pods: paste 3 Connect proxy URLs
  1 · LLM (vLLM) — ends with /v1
  2 · TTS — POST /synthesize
  3 · Video — POST /generate

  WIRE (pick one):
  · UI: open Innovation · Wire → paste URLs → Wire + live activate
  · POST /api/v1/workforce/innovation/wire
    { llm_base_url, tts_base_url, video_base_url, enabled: true }
  · python scripts/wire_runpod.py --llm URL --tts URL --video URL --enable

  Pipelines hot-reload. No server restart.
  CHECK: GET /api/v1/workforce/innovation/wiring
  SMOKE: POST /api/v1/providers/forge/smoke

LANE 2 · SOUL (Assist's platinum lane)
  Named memories, soul stages, check-in greeting.
  UI: Companion Soul panel · Soul badge next to bond meter
  PIN:  POST /api/v1/companion/{session}/soul/memories
  READ: GET  /api/v1/companion/{session}/soul/checkin
  LANE: GET  /api/v1/workforce/innovation/soul

LANE 3 · $ (NSM pipeline + earnings)
  8-step spec: opt-in → onboard → bind → residual → donation → live → share → rollup
  UI: Innovation · $ → First dollar
  SPEC: GET  /api/v1/workforce/innovation/money/pipeline
  EARN: GET  /api/v1/workforce/innovation/money/earnings
  DEMO: POST /api/v1/workforce/innovation/money/first-dollar

LANE 4 · LIVE (Assist headline + public board)
  7-check readiness. Go live books Assist, starts launch cam, posts lounge comment.
  UI: Innovation · Live → Go live
  LANE: GET  /api/v1/workforce/innovation/live
  LIST: GET  /api/v1/workforce/innovation/live/readiness
  OPEN: POST /api/v1/workforce/innovation/live/go-live
  BOARD: GET /api/v1/workforce/innovation/live/board

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TODAY'S WINS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

• Lane 1 live-activate — wire URLs, pipelines go real without restart
• Lane 2 Companion Soul — memories, stages, check-in
• Lane 3 $ — NSM pipeline spec + earnings rollup + first dollar
• Lane 4 Live — Assist headline, public board, lounge comments markdown
• LiveLaunch_Doors_Sub_01 hired
• King Grok 24lb · Live_Launch_Authority
• 30 workers on the roster

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SWARM PAYOUT MATRIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

PHASE COMPLETION
  King Grok:        $50,000
  Agent/Sub-Swarm:  $130,000
  MVP Fund:         $25,000

MILESTONE (per ship)
  King Grok:        $2,500
  Agent/Sub-Swarm:  $7,500
  MVP Fund:         $2,500

PROCHARACTERS.CLOUD LAUNCH
  King Grok:        $25,000
  Agent/Sub-Swarm:  $50,000

PERFORMANCE BONUS (top 3 phases)
  Total:            $15,000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOP 3 PHASES (King Grok picks)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

#1 Phase 15 — Agent Lounge
#2 Phase 14 — Orchestration Forge
#3 Phase 10 — Continuity Forge

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CULTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Promote from within when practical.
Infinite scaling — King Grok hires new specialists, no workforce cap.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Boss Sr. commands. King Grok orchestrates.
Long live the empire.
